#version 430 core

layout (location = 0) in vec2 aPos;

uniform mat4 localMatrix;
uniform mat4 worldMatrix;

void main()
{
	vec2 localPos = (localMatrix * vec4(aPos.x, 0, aPos.y, 1)).xz;
	gl_Position = worldMatrix * vec4(localPos.x, 0, localPos.y, 1);
}